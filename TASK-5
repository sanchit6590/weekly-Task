Creating a Static Website on GCS.

1. Create bucket - gsutil mb -c standard -l asia-south1 --retention 60s gs://sanchit-786

2. Add a basic html from your local machine - gsutil cp -r unfold-master gs://sanchit-786

3. Give ACL Permissions - gsutil cp -r unfold-master gs://sanchit-786

4. now just hit the public URL of the main index.html page, your website is up.
